{
  "name": "kramola",
  "version": "1.0.0",
  "description": "Kramola project with Vite bundler",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "watch": "npm run dev",
    "build": "vite build",
    "preview": "vite preview",
    "build:admin": "npm run build --prefix admin_ui",
    "watch:admin": "npm run watch --prefix admin_ui"
  },
  "dependencies": {
    "just-validate": "^4.3.0",
    "socket.io-client": "^4.8.1",
    "umbrellajs": "^3.3.3"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
